{
  "name": "blockchain-auth-demo",
  "version": "0.0.1",
  "description": "",
  "author": "",
  "private": true,
  "license": "UNLICENSED",
  "scripts": {
    "build": "pnpm turbo build",
    "build:packages": "pnpm turbo build --filter='./packages/**'",
    "build:clean": "pnpm clean && pnpm install && pnpm build",
    "dev:backend": "pnpm turbo dev --filter=@demo/backend",
    "dev": "pnpm dev:backend --filter=@demo/frontend",
    "clean": "pnpm turbo clean && rm -rf node_modules"
  },
  "packageManager": "pnpm@10.8.1",
  "devDependencies": {
    "@types/node": "^22",
    "turbo": "^2.5.0",
    "typescript": "^5"
  }
}
